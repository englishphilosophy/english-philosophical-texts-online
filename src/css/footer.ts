import * as colours from '../style/colours.ts'

export default `
footer {
  background: ${colours.darkgray};
  color: ${colours.white};
}

footer section {
  max-width: 86em;
  margin: 0 auto;
  padding: 1em;
}
`
